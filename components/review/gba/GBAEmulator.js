import {
    AudioSimpleBuffer,
    AudioBufferWrapper,
    GlueCodeMixer,
    GlueCodeMixerInput
} from "./iodinegba-master/user_scripts/AudioGlueCode"


export default function GBAEmulator(params) {

    // const audioGlueCode = new AudioSimpleBuffer();
    // const audioBufferWrapper = new AudioBufferWrapper();
    // const glueCodeMixer = new GlueCodeMixer();
    // const glueCodeMixerInput = new GlueCodeMixerInput();

    return (<>

    </>)
}